# CGP Production promotion manifest — stopped-state continuation (2026-09-25)

Original source: Production `ahfindosbawqfvhbcplq` application migration head `20260921205600` (60 versions). **Current stopped state:** the first six versions below are registered in Production; its head is `20260922182000` (66 versions). Migration 7 failed before registration and has no observed partial effect. Versions 7–14 are pending. This document does not authorize their execution.

The certified full sequence and unchanged SQL hashes follow. **Do not re-run entries 1–6.** Under a separate approval, apply **only entries 7–14**, in order, after the stopped-state pre-write gate. Verify each file's SHA-256 and its absent migration version immediately before execution. The supported customer migration session is `postgres`, including for `ALTER POLICY` on `storage.objects`; Supabase Support ticket `SU-483164` confirmed the hosted `supautils.policy_grants` path. Use versioned Supabase CLI migrations so it registers each successful file under its timestamp version. Do not use `supabase_admin`, `SET ROLE`, role membership grants, or managed-object ownership changes.

1. `20260922010000` — `20260922010000_prod_safe_review_schedule_open_cycle_guard.sql` — SHA-256 `d48d92a9af5c14e3eae80f016b1856d44b6a4cf7a562aa88f691a6e844163c23` — runner `postgres`.
2. `20260922020000` — `20260922020000_official_catalog_hierarchy_schema.sql` — SHA-256 `47a56149300a6f070ed268891f7ffede64d1131fca60fa5213ef0637f73800b9` — runner `postgres`.
3. `20260922030000` — `20260922030000_official_catalog_data_correction.sql` — SHA-256 `d48d957b54af0095313df5bcda322252f24a955081c85d7cf587edd9ed2be621` — runner `postgres`.
4. `20260922041000` — `20260922041000_prod_deterministic_mapping_review.sql` — SHA-256 `223f0658e13f75b6e3ae0a511af0daa075a4e62d65d3b420dc03412f6ad6df46` — runner `postgres`.
5. `20260922050000` — `20260922050000_official_catalog_display_title_fix.sql` — SHA-256 `99a3cfd9c57ab1af3d0ab80ad230abd858b547cbc72a8ac0ed4543f94ade7446` — runner `postgres`.
6. `20260922182000` — `20260922182000_regulatory_catalog_integrity_hardening.sql` — SHA-256 `447d3e5fe871b360052e9f270bf3faf06e6cb3887b076e9b02c7dd18869fc56c` — runner `postgres`.
7. `20260922183000` — `20260922183000_prod_legacy_ecc_reference_transition.sql` — SHA-256 `40c369f8073ef137b355c5ec7bd42357c205c041cef52aa10ad213a4bb27584a` — runner `postgres`.
8. `20260922184730` — `20260922184730_ecc_current_source_reconciliation.sql` — SHA-256 `ea2e840029395874e40eab7b8d671172d8748a35f6b2a356b72c91e4ca217802` — runner `postgres`.
9. `20260922193112` — `20260922193112_isolate_archived_regulatory_controls.sql` — SHA-256 `0a2f4217c1970ac0a058113aa29722726258250ef03c8190a6dc704cc73fdc3a` — runner `postgres`.
10. `20260922194306` — `20260922194306_guard_archived_indirect_activity.sql` — SHA-256 `982815ee2cb06f295fd79b00cf52978930559d28a1cb406d87c0e10126aafef0` — runner `postgres`.
11. `20260922200059` — `20260922200059_superseded_requirement_mapping_lifecycle.sql` — SHA-256 `5f481d8a9a2b79049b8d48c9f9310e103ab0aacbb49b57e016a9f88c4afb4106` — runner `postgres`.
12. `20260922225351` — `20260922225351_20260922201500_cscc_asset_display_title_correction.sql` — SHA-256 `68c52dba8d5ef7aef53538e2268fd0b34c2c7ca881306de89f2ef867caf596d0` — runner `postgres`.
13. `20260922230019` — `20260922230019_ccc_official_display_title_normalization.sql` — SHA-256 `3aace89c5324ed87410ea332f0c2f871dc281374a469a8be56ba02be3bdce5e9` — runner `postgres`.
14. `20260922230300` — `20260922230300_storage_policy_authenticated_scope.sql` — SHA-256 `f5d7b79e749e9854bfb50c3ca27e6c81cb4b49ab00a98ec63e8e4a7a73ebb220` — runner `postgres`.

Exclusions: `20260922000000_review_schedule_open_cycle_guard.sql` is QA-only. `20260922040000_mapping_review_refinement.sql` was already applied to QA and is immutable; Production uses the natural-key-ordered equivalent `20260922041000` instead. Do not register either exclusion in Production.

The Production-only `20260922183000` bridge must execute before the immutable QA-applied ECC `20260922184730` migration: it preserves and rejects eight references to the old ECC `2-7-3` identity before that migration's zero-reference check. QA did not apply this bridge because its old identity had no such references. Do not claim it was applied to QA.

QA applied the last three corrective migrations under the versions shown above. Their local file contents match the SQL recorded in QA history. Existing QA historical version `20260922184730` remains unchanged (SHA-256 `ea2e840029395874e40eab7b8d671172d8748a35f6b2a356b72c91e4ca217802`).

## Certified CLI execution route from 6/14 — not authorized by this document

**Passwordless CLI is prohibited for this release.** The prior project-ref/passwordless attempt authenticated as the temporary `cli_login_postgres` login. Migration 7 and the A-04 catalog-maintenance guard require both `current_user` and `session_user` to be `postgres`; migration 7 therefore failed without registration. The operator has separately verified that the Production **Direct Connection**, authenticated with the customer database password, returns `current_user / session_user / current_role = postgres / postgres / postgres` in a read-only probe. Do not put the password, a password-bearing URL, or probe output containing secrets in this manifest, a log, a command line, or Git. Use the operator-controlled secure credential channel for the same password-authenticated Direct Connection during any future CLI execution. Do not fall back to the temporary login-role path. Support ticket `SU-483164` remains the basis for customer-`postgres` execution; it does not waive the identity gate.

The repository migration directory is **not** a safe CLI input: it includes the two excluded QA migrations, and many already-applied Production versions have no local SQL file. Supabase CLI 2.117.0 rejects a temporary directory containing only the pending files because historical versions are missing locally; `--include-all` does not override that check.

For a separately authorized Production run, create a disposable workspace **outside Git** with `supabase/migrations/` containing:

- one fail-closed historical guard file for each of the exact **66** versions already registered in Production, including entries 1–6 above, named with its original timestamp; its SQL must raise an exception if ever executed;
- exactly the **eight** pending SQL files listed above as entries 7–14, byte-for-byte and hash-verified; no other unregistered migration, seed, or roles file.

The historical guards are **not** a claim that their SQL was replayed and must never be registered anew. Before any write, establish a **fresh recovery point for the current 6/14 state**, recheck database/Auth and actual Storage-content recovery, compare Production's ledger with exactly 66 registered versions (head `20260922182000`, entries 1–6 present, entries 7–14 absent), and recheck all 14 certified file hashes. Re-run a read-only identity probe through the **exact password-authenticated Direct Connection mechanism** intended for the CLI: `select current_user, session_user, current_role` must return `postgres / postgres / postgres`; any other result is a STOP. Run `supabase db push --dry-run --skip-vault` against the isolated workspace using that same authentication mechanism and require **exactly entries 7–14 in order**, with no QA-only, seed, roles, or historical files pending. For per-migration gates, expose one pending certified file at a time, dry-run to confirm exactly one pending version, apply through the CLI as the authenticated customer `postgres`, and verify its ledger entry and post-step invariant before exposing the next. A failed gate requires an immediate stop; do not repair history or retry automatically.

On 2026-09-25, two independent isolated local copies of the certified original Production baseline were advanced to the current six-migration state. Their catalog/mapping and structural/security fingerprints matched read-only Production; the ten later Production `feedback` rows are business-data-only drift, contain no control reference, and are not reproduced in the local copies. Entries 7–14 then succeeded unchanged under local `postgres / postgres / postgres` sessions. Both final states matched each other and the earlier certified full-14 CLI replay for migration history, schema/security/catalog/mapping and sequence fingerprints, ECC `108/92`, eight rejected historical references, mappings `235/205/30`, and the other certified target invariants. This was **local SQL replay**, not a Production CLI push; the Production-connected password-authenticated CLI dry-run and fresh recovery gate remain mandatory before any execution approval.

Never run raw `supabase db push` against this repository directory, use `--include-all`, repair Production migration history, or substitute a direct SQL runner for Production execution. Do not print the connection URL or credentials. Halt if Production's head, any file hash, expected baseline cardinality, Storage policy-grant configuration, or the eight reviewed reference set differs. Do not infer successor mappings. Obtain separate approval before any Production write.
