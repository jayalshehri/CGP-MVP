"use client";

import { FormEvent, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";
import { WorkflowHeading, WorkflowMetric, ResultSummary } from "@/components/WorkflowUI";
import { requireProfile, type UserRole } from "@/lib/auth";
import { supabase } from "@/lib/supabase";
import "./vulnerabilities.css";

type Vulnerability={id:number;vulnerability_code:string;title:string;description:string|null;vendor_url:string|null;cve_id:string|null;cvss_score:number|null;affected_technologies:string|null;affected_assets:string|null;threat_analysis:string|null;threat_severity:number;likelihood:number;risk_severity:number;risk_score:number;owner_name:string|null;status:string;first_observed_date:string;due_date:string|null;resolved_date:string|null;notes:string|null;linked_control_id:number|null;updated_at:string};
type Control={id:number;control_code:string;title_ar:string;frameworks:unknown};
const statusLabels:Record<string,string>={open:"مفتوح",in_progress:"جارٍ العمل عليه",on_hold:"معلّق",resolved:"مغلق",exception:"استثناء معتمد"};
const emptyForm={vulnerability_code:"",title:"",description:"",vendor_url:"",cve_id:"",cvss_score:"",affected_technologies:"",affected_assets:"",threat_analysis:"",threat_severity:"3",likelihood:"3",risk_severity:"3",owner_name:"",status:"open",first_observed_date:new Date().toLocaleDateString("en-CA",{timeZone:"Asia/Riyadh"}),due_date:"",resolved_date:"",notes:"",linked_control_id:""};
const riskLabel=(score:number)=>score<=3?"منخفض جدًا":score<=6?"منخفض":score<=10?"متوسط":score<=16?"مرتفع":"مرتفع جدًا";
const riskTone=(score:number)=>score>=17?"critical":score>=11?"high":score>=7?"medium":"low";

export default function VulnerabilitiesPage(){
 const router=useRouter();const [items,setItems]=useState<Vulnerability[]>([]),[controls,setControls]=useState<Control[]>([]),[role,setRole]=useState<UserRole>("control_owner"),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[message,setMessage]=useState(""),[showForm,setShowForm]=useState(false),[search,setSearch]=useState(""),[filter,setFilter]=useState("active"),[form,setForm]=useState(emptyForm);
 async function load(){const [{data,error:e},{data:c,error:ce}]=await Promise.all([supabase.from("vulnerabilities").select("*").order("risk_score",{ascending:false}).order("due_date",{ascending:true,nullsFirst:false}),supabase.from("controls").select("id,control_code,title_ar,frameworks!inner(code)").order("id")]);if(e||ce)throw e||ce;setItems((data??[]) as Vulnerability[]);setControls((c??[]) as Control[])}
 useEffect(()=>{let active=true;(async()=>{try{const {profile}=await requireProfile();if(!active)return;setRole(profile.role);await load()}catch(e){if(!active)return;const m=e instanceof Error?e.message:e&&typeof e==="object"&&"message" in e?String((e as {message?:unknown}).message??""):"";if(m.includes("تسجيل الدخول")){router.replace("/login");return}const databasePending=!m||m.includes("vulnerabilities")||m.includes("schema cache")||m.includes("relation");setError(databasePending?"سجل الثغرات جاهز وينتظر تفعيل قاعدة البيانات للنسخة التجريبية.":"تعذر تحميل سجل الثغرات. حاول التحديث مرة أخرى.")}finally{if(active)setLoading(false)}})();return()=>{active=false}},[router]);
 const activeItems=items.filter(v=>!["resolved","exception"].includes(v.status));const today=new Date().toLocaleDateString("en-CA",{timeZone:"Asia/Riyadh"});const overdue=activeItems.filter(v=>v.due_date&&v.due_date<today).length;
 const visible=useMemo(()=>items.filter(v=>{const q=search.trim().toLowerCase();const matches=!q||`${v.vulnerability_code} ${v.title} ${v.cve_id||""} ${v.owner_name||""} ${v.affected_assets||""}`.toLowerCase().includes(q);const status=filter==="all"||filter==="active"&&!["resolved","exception"].includes(v.status)||v.status===filter;return matches&&status}),[items,search,filter]);
 async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError("");setMessage("");try{const {user}=await requireProfile(["admin","cybersecurity_team"]);const payload={...form,cvss_score:form.cvss_score?Number(form.cvss_score):null,threat_severity:Number(form.threat_severity),likelihood:Number(form.likelihood),risk_severity:Number(form.risk_severity),due_date:form.due_date||null,resolved_date:form.resolved_date||null,linked_control_id:form.linked_control_id?Number(form.linked_control_id):null,vendor_url:form.vendor_url||null,cve_id:form.cve_id||null,created_by:user.id};const {error:e}=await supabase.from("vulnerabilities").insert(payload);if(e)throw e;await load();setForm(emptyForm);setShowForm(false);setMessage("تمت إضافة الثغرة إلى السجل.")}catch(e){setError(e instanceof Error?e.message:"تعذر حفظ الثغرة")}finally{setSaving(false)}}
 async function updateStatus(id:number,status:string){setError("");const patch={status,resolved_date:status==="resolved"?today:null,updated_at:new Date().toISOString()};const {error:e}=await supabase.from("vulnerabilities").update(patch).eq("id",id);if(e){setError("تعذر تحديث حالة الثغرة.");return}setItems(rows=>rows.map(v=>v.id===id?{...v,...patch}:v))}
 if(loading)return <main className="workflow-page" role="status">جاري تحميل سجل الثغرات…</main>;
 return <main className="workflow-page vulnerability-page" dir="rtl"><WorkflowHeading title="سجل الثغرات" description="متابعة الثغرات والأصول المتأثرة والأولوية والمعالجة وفق نموذج NCA." action={role!=="control_owner"?<button className="workflow-button workflow-primary" onClick={()=>setShowForm(v=>!v)}>+ تسجيل ثغرة</button>:undefined}/>
 {error&&<p className="cgp-shell-error" role="alert">{error}</p>}{message&&<p className="workflow-success" role="status">{message}</p>}
 <div className="workflow-metrics"><WorkflowMetric label="إجمالي الثغرات" value={items.length}/><WorkflowMetric label="مفتوحة" value={activeItems.length} tone="warning"/><WorkflowMetric label="مرتفعة جدًا" value={activeItems.filter(v=>v.risk_score>=17).length} tone="danger"/><WorkflowMetric label="متأخرة" value={overdue} tone={overdue?"danger":"neutral"}/></div>
 {showForm&&<form className="vulnerability-form" onSubmit={submit}><header><h2>تسجيل ثغرة جديدة</h2><p>الحقول مبنية على نموذج سجل الثغرات الصادر عن NCA.</p></header><div className="vulnerability-fields"><label>رقم تعريف الثغرة<input required value={form.vulnerability_code} onChange={e=>setForm({...form,vulnerability_code:e.target.value})} placeholder="VULN-001"/></label><label>العنوان<input required value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>رقم CVE<input dir="ltr" value={form.cve_id} onChange={e=>setForm({...form,cve_id:e.target.value})} placeholder="CVE-2026-0000"/></label><label>درجة CVSS<input type="number" min="0" max="10" step="0.1" value={form.cvss_score} onChange={e=>setForm({...form,cvss_score:e.target.value})}/></label><label className="wide">وصف الثغرة<textarea required rows={3} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label className="wide">الأصول المتأثرة<textarea rows={3} value={form.affected_assets} onChange={e=>setForm({...form,affected_assets:e.target.value})}/></label><label>التقنيات المتأثرة<input value={form.affected_technologies} onChange={e=>setForm({...form,affected_technologies:e.target.value})}/></label><label>المالك<input value={form.owner_name} onChange={e=>setForm({...form,owner_name:e.target.value})}/></label><label>الاحتمالية<select value={form.likelihood} onChange={e=>setForm({...form,likelihood:e.target.value})}>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select></label><label>شدة المخاطر<select value={form.risk_severity} onChange={e=>setForm({...form,risk_severity:e.target.value})}>{[1,2,3,4,5].map(n=><option key={n}>{n}</option>)}</select></label><label>تاريخ الملاحظة الأولى<input type="date" required value={form.first_observed_date} onChange={e=>setForm({...form,first_observed_date:e.target.value})}/></label><label>الموعد النهائي<input type="date" value={form.due_date} onChange={e=>setForm({...form,due_date:e.target.value})}/></label><label>الضابط المرتبط<select value={form.linked_control_id} onChange={e=>setForm({...form,linked_control_id:e.target.value})}><option value="">بدون ربط</option>{controls.map(c=><option key={c.id} value={c.id}>{c.control_code} — {c.title_ar}</option>)}</select></label><label>رابط المورد<input dir="ltr" type="url" value={form.vendor_url} onChange={e=>setForm({...form,vendor_url:e.target.value})}/></label><label className="wide">تحليل التهديدات<textarea rows={3} value={form.threat_analysis} onChange={e=>setForm({...form,threat_analysis:e.target.value})}/></label><label className="wide">ملاحظات المعالجة<textarea rows={3} value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label></div><div className={`vulnerability-risk ${riskTone(Number(form.likelihood)*Number(form.risk_severity))}`}><span>مستوى المخاطر المحسوب</span><strong>{Number(form.likelihood)*Number(form.risk_severity)}/25</strong><b>{riskLabel(Number(form.likelihood)*Number(form.risk_severity))}</b></div><div className="third-party-actions"><button disabled={saving} className="workflow-button workflow-primary">{saving?"جاري الحفظ…":"حفظ الثغرة"}</button><button type="button" className="workflow-button" onClick={()=>setShowForm(false)}>إلغاء</button></div></form>}
 <div className="workflow-filter workflow-filter-grid"><label>البحث<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="رقم الثغرة، CVE، الأصل أو المالك"/></label><label>الحالة<select value={filter} onChange={e=>setFilter(e.target.value)}><option value="active">الثغرات النشطة</option><option value="all">جميع الحالات</option>{Object.entries(statusLabels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></label></div><ResultSummary count={visible.length} total={items.length} active={Boolean(search)||filter!=="all"} reset={()=>{setSearch("");setFilter("all")}}/>
 {!visible.length?<div className="workflow-empty">لا توجد ثغرات مطابقة في السجل.</div>:<div className="vulnerability-table-wrap"><table className="vulnerability-table"><thead><tr><th>الثغرة</th><th>الأصول المتأثرة</th><th>المخاطر</th><th>المالك</th><th>الموعد</th><th>الحالة</th></tr></thead><tbody>{visible.map(v=><tr key={v.id}><td><b dir="ltr">{v.vulnerability_code}</b><strong>{v.title}</strong>{v.cve_id&&<small dir="ltr">{v.cve_id}{v.cvss_score!==null?` · CVSS ${v.cvss_score}`:""}</small>}</td><td>{v.affected_assets||"غير محددة"}</td><td><span className={`risk-pill risk-${riskTone(v.risk_score)}`}>{riskLabel(v.risk_score)} · {v.risk_score}/25</span></td><td>{v.owner_name||"غير معيّن"}</td><td className={v.due_date&&v.due_date<today&&!["resolved","exception"].includes(v.status)?"overdue":""}>{v.due_date||"غير محدد"}</td><td>{role==="control_owner"?<span>{statusLabels[v.status]}</span>:<select aria-label={`حالة ${v.vulnerability_code}`} value={v.status} onChange={e=>void updateStatus(v.id,e.target.value)}>{Object.entries(statusLabels).map(([code,label])=><option key={code} value={code}>{label}</option>)}</select>}</td></tr>)}</tbody></table></div>}
 </main>
}
