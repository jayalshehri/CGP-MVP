'use client';
import AssessmentPortfolio from '@/components/AssessmentPortfolio';
import Link from 'next/link';
import {useCallback,useEffect,useMemo,useState} from 'react';
import {requireProfile,type UserRole} from '@/lib/auth';
import {supabase} from '@/lib/supabase';
import {WorkflowHeading,WorkflowMetric} from '@/components/WorkflowUI';
import {type ReviewCycle,frequencyLabels,todayRiyadh,formatGrcDate,csvDownload} from '@/lib/grc';
import './audit-schedule.css';
type Control={id:number;control_code:string;title_ar:string;control_owner:string|null;control_owner_id:string|null;audit_frequency:string;last_audit_date:string|null;next_audit_date:string|null;frameworks:{code:string}|null};
type Person={user_id:string;display_name:string|null};
function state(date:string|null){if(!date)return 'unscheduled';const today=todayRiyadh();const limit=new Date(`${today}T12:00:00+03:00`);limit.setUTCDate(limit.getUTCDate()+30);return date<today?'overdue':date<=limit.toISOString().slice(0,10)?'upcoming':'scheduled';}
const states:Record<string,string>={overdue:'متأخر',upcoming:'خلال 30 يومًا',scheduled:'مجدول',unscheduled:'غير مجدول'};
export default function AuditSchedulePage(){
 const [controls,setControls]=useState<Control[]>([]),[cycles,setCycles]=useState<ReviewCycle[]>([]),[people,setPeople]=useState<Person[]>([]),[role,setRole]=useState<UserRole|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState('');
 const [framework,setFramework]=useState('all'),[frequencyFilter,setFrequencyFilter]=useState('all'),[stateFilter,setStateFilter]=useState('open'),[search,setSearch]=useState('');
 const [editing,setEditing]=useState<Control|null>(null),[reviewer,setReviewer]=useState(''),[frequency,setFrequency]=useState('annual'),[due,setDue]=useState(''),[requirement,setRequirement]=useState(''),[reason,setReason]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState('');
 const canManage=role==='admin'||role==='cybersecurity_team';
 const load=useCallback(async()=>{try{const {profile}=await requireProfile();setRole(profile.role);const [c,cy]=await Promise.all([supabase.from('controls').select('id,control_code,title_ar,control_owner,control_owner_id,audit_frequency,last_audit_date,next_audit_date,frameworks(code)').order('id'),supabase.from('control_review_cycles').select('*').order('id',{ascending:false})]);if(c.error||cy.error)throw c.error||cy.error;setControls((c.data??[]).map(row=>({...row,frameworks:Array.isArray(row.frameworks)?row.frameworks[0]??null:row.frameworks})) as Control[]);setCycles(cy.data??[]);if(['admin','cybersecurity_team'].includes(profile.role)){const p=await supabase.from('profiles').select('user_id,display_name').in('role',['admin','cybersecurity_team']).eq('is_active',true);if(p.error)throw p.error;setPeople(p.data??[]);}}catch(e){setError(e instanceof Error?e.message:'تعذر تحميل الجدول');}finally{setLoading(false);}},[]);
 useEffect(()=>{const t=setTimeout(()=>{void load();},0);return()=>clearTimeout(t);},[load]);
 const filtered=useMemo(()=>controls.filter(c=>(framework==='all'||c.frameworks?.code===framework)&&(frequencyFilter==='all'||c.audit_frequency===frequencyFilter)&&(stateFilter==='all'||(stateFilter==='open'?['overdue','upcoming','unscheduled'].includes(state(c.next_audit_date)):state(c.next_audit_date)===stateFilter))&&`${c.control_code} ${c.title_ar} ${c.control_owner}`.includes(search)),[controls,framework,frequencyFilter,stateFilter,search]);
 const frameworks=[...new Set(controls.map(c=>c.frameworks?.code).filter(Boolean))];
 const openCycle=editing?cycles.find(c=>c.control_id===editing.id&&c.status==='open'):undefined;
 function edit(c:Control){const cycle=cycles.find(x=>x.control_id===c.id&&x.status==='open');setEditing(c);setReviewer(cycle?.reviewer_id??'');setDue(cycle?.due_date??c.next_audit_date??'');setFrequency(cycle?.frequency??c.audit_frequency);setRequirement(c.title_ar);setReason('');setError('');}
 async function save(){if(!editing||busy)return;setBusy(true);setError('');const r=await supabase.rpc('cgp_grc_command',{p_action:openCycle?'reschedule':'open_cycle',p_control_id:editing.id,p_data:{cycle_id:openCycle?.id,reviewer_id:reviewer,due_date:due,frequency,requirement,reason}});if(r.error)setError(r.error.message);else{setMessage(openCycle?'تم تحديث الدورة وتسجيل التغيير.':'تم إنشاء الدورة وطلب الدليل.');setEditing(null);await load();}setBusy(false);}
 if(loading)return <main role="status">جاري تحميل جدول المراجعات…</main>;
 return <main className="workflow-page audit-schedule-page" dir="rtl"><WorkflowHeading title="جدول التدقيق الدوري" description="دورات مراجعة محفوظة وفق سياسة الجهة. إغلاق الدورة يتطلب قرار تقييم، وينشئ الدورة التالية."/>{error&&<p role="alert" className="cgp-shell-error">{error}</p>}{message&&<p role="status">{message}</p>}<div className="workflow-metrics">{['overdue','upcoming','unscheduled'].map(k=><WorkflowMetric key={k} label={states[k]} value={controls.filter(c=>state(c.next_audit_date)===k).length}/>)}<WorkflowMetric label="دورات مكتملة" value={cycles.filter(c=>c.status==='completed').length}/></div><section className="audit-filter-bar"><label>الإطار<select value={framework} onChange={e=>setFramework(e.target.value)}><option value="all">جميع الأطر</option>{frameworks.map(f=><option key={f}>{f}</option>)}</select></label><label>التكرار<select value={frequencyFilter} onChange={e=>setFrequencyFilter(e.target.value)}><option value="all">كل التكرارات</option>{Object.entries(frequencyLabels).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label><label>الحالة<select value={stateFilter} onChange={e=>setStateFilter(e.target.value)}><option value="open">تحتاج متابعة</option><option value="all">كل الحالات</option>{Object.entries(states).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label></section><div className="workflow-filter"><label>بحث<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="رقم الضابط أو اسمه أو المالك"/></label><button onClick={()=>window.print()}>طباعة / PDF</button><button onClick={()=>csvDownload('periodic-reviews.csv',[['الإطار','الضابط','الاسم','المالك','التكرار','آخر مراجعة','الموعد القادم','حالة الموعد'],...filtered.map(c=>[c.frameworks?.code??'',c.control_code,c.title_ar,c.control_owner??'',frequencyLabels[c.audit_frequency],c.last_audit_date??'',c.next_audit_date??'',states[state(c.next_audit_date)]])])}>تصدير CSV</button></div><section className="audit-table-wrap"><table className="audit-table"><thead><tr><th>الضابط</th><th>الإطار</th><th>المالك / المراجع</th><th>التكرار</th><th>آخر مراجعة</th><th>القادمة</th><th>الحالة</th><th>الإجراء</th></tr></thead><tbody>{filtered.map(c=>{const cycle=cycles.find(x=>x.control_id===c.id&&x.status==='open');return <tr key={c.id}><td><strong dir="ltr">{c.control_code}</strong><span>{c.title_ar}</span></td><td>{c.frameworks?.code}</td><td>{c.control_owner??'غير معيّن'}<br/>{cycle?(people.find(p=>p.user_id===cycle.reviewer_id)?.display_name??'مراجع مكلف'):'دون دورة مفتوحة'}</td><td>{frequencyLabels[c.audit_frequency]}</td><td>{formatGrcDate(c.last_audit_date)}</td><td>{formatGrcDate(c.next_audit_date)}</td><td><span className={`audit-state ${state(c.next_audit_date)}`}>{states[state(c.next_audit_date)]}</span></td><td><div className="audit-actions"><Link href={`/controls/${c.id}`}>فتح</Link>{canManage&&<button onClick={()=>edit(c)}>{cycle?'تعديل الدورة':'إنشاء دورة'}</button>}</div></td></tr>;})}</tbody></table>{!filtered.length&&<p>لا توجد نتائج مطابقة.</p>}</section><details className="grc-cycle-history"><summary>تاريخ الدورات المكتملة</summary>{cycles.filter(c=>c.status==='completed').map(c=><p key={c.id}><Link href={`/controls/${c.control_id}`}>دورة #{c.id}</Link> · {formatGrcDate(c.completed_at)} · {c.notes}</p>)}</details>
 {editing&&<dialog ref={node=>{if(node&&!node.open)node.showModal();}} onCancel={e=>{e.preventDefault();if(!busy)setEditing(null);}} className="audit-editor" aria-labelledby="cycle-title"><form className="grc-form" onSubmit={e=>{e.preventDefault();void save();}}>{error&&<p role="alert">{error}</p>}<h2 id="cycle-title">{openCycle?'تعديل الدورة':'إنشاء دورة وطلب دليل'} · {editing.control_code}</h2>{!editing.control_owner_id&&<p className="grc-warning">عيّن مالك الضابط أولًا. <Link href={`/controls/${editing.id}/assign`}>تعيين مالك</Link></p>}<label>المراجع<select required value={reviewer} onChange={e=>setReviewer(e.target.value)}><option value="">اختر مراجعًا</option>{people.filter(p=>p.user_id!==editing.control_owner_id).map(p=><option key={p.user_id} value={p.user_id}>{p.display_name??p.user_id}</option>)}</select></label><label>موعد الاستحقاق<input required type="date" value={due} onChange={e=>setDue(e.target.value)}/></label><label>التكرار<select value={frequency} onChange={e=>setFrequency(e.target.value)}>{Object.entries(frequencyLabels).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></label>{openCycle?<label>سبب التعديل<textarea required value={reason} onChange={e=>setReason(e.target.value)}/></label>:<label>الأدلة المطلوبة<textarea required value={requirement} onChange={e=>setRequirement(e.target.value)}/></label>}<div className="grc-inline-actions"><button className="audit-save" disabled={busy||!editing.control_owner_id}>حفظ</button><button type="button" className="audit-cancel" disabled={busy} onClick={()=>setEditing(null)}>إلغاء</button></div></form></dialog>}
 <AssessmentPortfolio view="schedule"/></main>;
}
